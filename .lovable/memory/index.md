# Project Memory

## Core
Rotas: criar arquivo em `src/routes/`; nunca editar `routeTree.gen.ts`; nunca migrar para SSR só por rota; nunca usar HTML estático em `public/` competindo com SPA.

## Memories
- [Routing constraints](mem://constraints/routing) — regras para adicionar rotas no TanStack sem quebrar produção
